# v0.1.0

- Исправлено: гостевой чат больше не создаётся заново при входе, если уже есть пустой.
- Оптимизация: ленивые загрузки (AuthModal), мемоизированный список чатов и устойчивые колбэки для меньшего числа ререндеров.
- CI: релизная action теперь подтягивает release notes по имени тега.

## Известные моменты
- Перед релизом убедитесь, что артефакты собраны (`npm run build:all` или матричные сборки CI) и secrets Supabase заданы в GitHub Actions.
