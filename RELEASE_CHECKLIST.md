# 📋 Release Checklist - Автоматический релиз

## 🚀 Быстрый старт (3 команды!)

### 1. Настройка GitHub Actions (один раз)

Добавьте секрет в GitHub:
1. Перейдите в Settings → Secrets and variables → Actions
2. Создайте `VITE_POLZA_API_KEY` с вашим API ключом

### 2. Создание релиза (автоматически!)

```bash
# Закоммитьте изменения
git add .
git commit -m "Release v0.0.5 - Tauri Migration"

# Создайте и отправьте тег
git tag v0.0.5
git push origin main
git push origin v0.0.5
```

### 3. Готово! ✨

GitHub Actions автоматически:
- ✅ Соберет macOS ARM64
- ✅ Соберет Windows x64
- ✅ Соберет Linux x64
- ✅ Создаст релиз на GitHub
- ✅ Загрузит все файлы в релиз
- ✅ Добавит описание из `RELEASE_NOTES_v0.0.5.md`

**Просто подождите ~10-15 минут и релиз готов!**

## 📦 Что будет в релизе

- `AI UML Builder_0.0.5_aarch64.dmg` - macOS ARM64 (~5 MB)
- `AI UML Builder_0.0.5_x64_en-US.msi` - Windows x64 (~10-20 MB)
- `ai-uml-builder_0.0.5_amd64.AppImage` - Linux x64 (~15-25 MB)
- `ai-uml-builder_0.0.5_amd64.deb` - Linux DEB package

## 🔍 Мониторинг процесса

1. Перейдите в **Actions** → **Build and Release**
2. Откройте запущенный workflow
3. Следите за прогрессом сборки каждой платформы
4. После завершения проверьте **Releases**

## 🐛 Если что-то пошло не так

### Сборка упала
- Проверьте логи в Actions
- Убедитесь, что `VITE_POLZA_API_KEY` добавлен в секреты
- Проверьте, что все файлы закоммичены

### Релиз не создался
- Убедитесь, что тег начинается с `v` (например, `v0.0.5`)
- Проверьте, что файл `RELEASE_NOTES_v0.0.5.md` существует

### Нужно пересобрать
```bash
# Удалите тег локально и на GitHub
git tag -d v0.0.5
git push origin :refs/tags/v0.0.5

# Создайте заново
git tag v0.0.5
git push origin v0.0.5
```

## 📝 Локальная сборка (опционально)

Если хотите протестировать локально перед релизом:

```bash
# Только macOS (на вашей машине)
npm run build:mac
```

Файл будет здесь: `src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/`

## Проверка после релиза

- [ ] Все файлы загружены
- [ ] Ссылки для скачивания работают
- [ ] README.md обновлен
- [ ] CHANGELOG.md обновлен (если есть)
- [ ] Размеры файлов корректны

## Размеры файлов (примерно)

- macOS: ~5 MB
- Windows: ~10-20 MB
- Linux: ~15-25 MB

## Примечания

### Кросс-компиляция
- macOS → Windows/Linux: Не работает напрямую
- Используйте GitHub Actions для сборки всех платформ

### Первая сборка
- Может занять 5-10 минут (компиляция Rust зависимостей)
- Последующие сборки быстрее благодаря кешу
