# Release Notes v0.1.6

FIX
- Updater UI: shows “самая актуальная версия” when нет обновлений and keeps the refresh icon static until реальная проверка/загрузка.
- Updater errors now surface the real message instead of generic “unknown”.
- Keep latest.json available for the release workflow to serve updates correctly.
